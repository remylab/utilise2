# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           		controllers.Application.index()
GET     /contact							controllers.Application.contact()
GET     /a-propos                           controllers.Application.about()
GET     /photos                             controllers.Application.pictures()
GET     /admin                              controllers.Application.admin()

POST    /login                              controllers.Membership.login()
POST    /addpost                            controllers.Admin.addPost()
GET     /public/javascript/routes   		controllers.Application.jsRoutes()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
